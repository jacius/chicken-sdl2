
(define-foreign-type unsigned-integer16 unsigned-short)

(define-foreign-type SDL_DisplayMode* (c-pointer "SDL_DisplayMode"))

(define-foreign-type SDL_GLContext c-pointer)

(define-foreign-type SDL_SysWMinfo* (c-pointer "SDL_SysWMinfo"))

(define-foreign-type SDL_Surface* (c-pointer "SDL_Surface"))

(define-foreign-type SDL_Texture* (c-pointer "SDL_Texture"))

(define-foreign-type SDL_Version* (c-pointer "SDL_version"))

(define-foreign-type SDL_Window* (c-pointer "SDL_Window"))

