
(define-foreign-type SDL_GLContext c-pointer)

(define-foreign-type SDL_Texture* (c-pointer "SDL_Texture"))

(define-foreign-type SDL_Version* (c-pointer "SDL_version"))

(define-foreign-type SDL_Window* (c-pointer "SDL_Window"))

